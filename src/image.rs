// use image::{ImageBuffer, Rgba};
// use std::error::Error;
// use crate::channel;


// async fn get_image() -> Result<Vec<u8>, Box<dyn Error>> {
//     let img = image::io::Reader::new(std::io::Cursor::new(&ch.image)).with_guessed_format()?.decode()?;
//     let img_buff = img.into_rgba8();
//     Ok( img_buff.as_raw().to_owned() )
// }
